import { MyDatePickerDirective } from './my-date-picker.directive';

describe('MyDatePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDatePickerDirective();
    expect(directive).toBeTruthy();
  });
});
