import { SelectBoxItDirective } from './select-box-it.directive';

describe('SelectBoxItDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectBoxItDirective();
    expect(directive).toBeTruthy();
  });
});
