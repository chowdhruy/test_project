import { WidgetCounterDirective } from './widget-counter.directive';

describe('WidgetCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetCounterDirective();
    expect(directive).toBeTruthy();
  });
});
